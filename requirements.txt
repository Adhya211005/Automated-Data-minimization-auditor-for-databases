# Auditor runtime deps. (Phase 0 seeder has its own seed-data/requirements.txt.)
SQLAlchemy>=2.0
psycopg[binary]>=3.1        # PostgreSQL driver (postgresql+psycopg)
sqlglot>=25                 # SQL parsing for the usage analyzer (Phase 3)
# PyMySQL>=1.1              # uncomment when MySQL target support lands

# dev / test
pytest>=8.0
