# Copy to seed-data/.env and adjust. .env is gitignored.
# The seed generator connects with these and creates the DB_NAME database
# if it does not exist (the role needs CREATEDB).
DB_HOST=localhost
DB_PORT=5432
DB_USER=dma_seed
DB_PASSWORD=dma_seed_local_pw
DB_NAME=dma_auditor
